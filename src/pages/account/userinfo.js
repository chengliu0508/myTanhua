import React from 'react';
import {Text, View} from 'react-native';
import {Avatar, Badge, Icon, withBadge} from 'react-native-elements';

const App = () => <Badge value={<Text>My Custom Badge</Text>} />;

export default App;
