import {Button, ThemeProvider} from 'react-native-elements';
import React from 'react';
const MyApp = () => {
  return (
    <ThemeProvider>
      <Button title="my!" />
    </ThemeProvider>
  );
};
export default MyApp;
